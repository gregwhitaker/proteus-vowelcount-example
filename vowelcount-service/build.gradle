plugins {
    id 'java'
    id 'application'
}

mainClassName = 'proteus.example.service.vowelcount.Main'
sourceCompatibility = 1.8

dependencies {
    compile project(':vowelcount-idl')

    compile "io.netifi.proteus:client:${proteusVersion}"
}
